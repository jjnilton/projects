import styled from 'styled-components';

const StyledSettings = styled.section``;

const Settings = () => {
  return (
    <StyledSettings>PT | BR | DARK | LIGHT</StyledSettings>
  )
}

export default Settings;